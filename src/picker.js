import React, { Component } from 'react';
import ReactDOM, { render as Render } from 'react-dom';

class PickerTrigger extends Component {
    render() {
        return (
            <div>hello, world</div>
        );
    }
}

// do not use 'export default Picker'
module.exports = PickerTrigger;